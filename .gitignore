__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Cached market data is regenerated on demand.
.cache/
*.pkl
*.parquet
site/

# data/backtest.json is deliberately committed: it is the track record the
# dashboard shows, it takes 40 minutes to regenerate, and a static build has
# nowhere to compute one.
!data/backtest.json

.DS_Store
.idea/
.vscode/
